set guifont=RictyDiminishedDiscord-Regular:h16,RictyDiminished-Regular:h16,Ricty-Regular:h16

if has('gui_macvim')
  set transparency=5 "透明度を指定
endif

set columns=170 "スクリーン上の列幅
set lines=150 "ウィンドウの高さ

colorscheme itg_flat

set imdisable
